export default function TrackLandingPage() {
  return (
    <div className="flex min-h-screen items-center justify-center bg-ink-50 px-4 text-center">
      <div>
        <h1 className="text-lg font-semibold text-ink-900">Order tracking</h1>
        <p className="mt-1 text-sm text-ink-500">
          Public tracking pages (e.g. <code>/track/ORD-1024</code>) arrive in Phase 2.
        </p>
      </div>
    </div>
  );
}
